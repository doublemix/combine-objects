'use strict';

/* eslint-disable */

var expect = require('chai').expect;
import combine from '../src/index';

const { opaque, replace, withScalrs, remove } = combine;

describe('combineObjects', function () {
    it('needs to be tested', function () {});
});
